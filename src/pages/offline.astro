---
/**
 * @file offline.astro
 * @description PWA offline fallback page
 *
 * This page is displayed when the user is offline and tries to access
 * a page that hasn't been cached by the service worker.
 */

import { Icon } from "astro-icon/components";

import Layout from "@/layouts/Layout.astro";

const title = "Offline - Keine Internetverbindung";
const description =
  "Du bist derzeit offline. Bitte überprüfe deine Internetverbindung.";
---

<Layout title={title} description={description}>
  <main
    class="container mx-auto flex min-h-[70vh] items-center justify-center px-4 py-16"
  >
    <div class="max-w-2xl text-center">
      <!-- Offline Icon -->
      <div class="mb-8 flex justify-center">
        <Icon
          name="tabler:wifi-off"
          class="text-muted-foreground h-24 w-24 opacity-50"
          aria-hidden="true"
        />
      </div>

      <!-- Heading -->
      <h1 class="mb-4 text-4xl font-bold text-foreground md:text-5xl">
        Du bist offline
      </h1>

      <!-- Description -->
      <p class="text-muted-foreground mb-8 text-lg md:text-xl">
        Es scheint, als hättest du keine Internetverbindung. Bitte überprüfe
        deine Verbindung und versuche es erneut.
      </p>

      <!-- Tips -->
      <div class="mb-8 rounded-lg bg-muted/50 p-6 text-left">
        <h2 class="mb-4 flex items-center gap-2 text-xl font-semibold">
          <Icon name="tabler:bulb" class="h-5 w-5" aria-hidden="true" />
          Tipps zur Fehlerbehebung
        </h2>
        <ul class="text-muted-foreground space-y-2">
          <li class="flex items-start gap-2">
            <Icon
              name="tabler:check"
              class="mt-0.5 h-5 w-5 shrink-0"
              aria-hidden="true"
            />
            <span>Überprüfe, ob dein WLAN oder mobile Daten aktiviert sind</span
            >
          </li>
          <li class="flex items-start gap-2">
            <Icon
              name="tabler:check"
              class="mt-0.5 h-5 w-5 shrink-0"
              aria-hidden="true"
            />
            <span>Stelle sicher, dass der Flugmodus deaktiviert ist</span>
          </li>
          <li class="flex items-start gap-2">
            <Icon
              name="tabler:check"
              class="mt-0.5 h-5 w-5 shrink-0"
              aria-hidden="true"
            />
            <span>Versuche, deine Internetverbindung neu zu starten</span>
          </li>
          <li class="flex items-start gap-2">
            <Icon
              name="tabler:check"
              class="mt-0.5 h-5 w-5 shrink-0"
              aria-hidden="true"
            />
            <span
              >Einige zuvor besuchte Seiten sind möglicherweise auch offline
              verfügbar</span
            >
          </li>
        </ul>
      </div>

      <!-- Actions -->
      <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
        <button
          onclick="window.location.reload()"
          class="bg-primary text-primary-foreground hover:bg-primary/90 inline-flex items-center gap-2 rounded-lg px-6 py-3 font-semibold transition-colors"
        >
          <Icon name="tabler:refresh" class="h-5 w-5" aria-hidden="true" />
          Erneut versuchen
        </button>

        <a
          href="/"
          class="bg-secondary text-secondary-foreground hover:bg-secondary/80 inline-flex items-center gap-2 rounded-lg px-6 py-3 font-semibold transition-colors"
        >
          <Icon name="tabler:home" class="h-5 w-5" aria-hidden="true" />
          Zur Startseite
        </a>
      </div>

      <!-- Additional Info -->
      <div class="mt-12 border-t border-border pt-8">
        <p class="text-muted-foreground text-sm">
          <Icon
            name="tabler:info-circle"
            class="mr-1 inline h-4 w-4"
            aria-hidden="true"
          />
          Diese App funktioniert auch offline für Seiten, die du bereits besucht
          hast.
        </p>
      </div>
    </div>
  </main>
</Layout>

<style>
  /* Ensure icons are properly displayed */
  [astro-icon] {
    display: inline-block;
    vertical-align: middle;
  }

  /* Add subtle animation to the main icon */
  [astro-icon].w-24 {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.5;
    }
    50% {
      opacity: 0.3;
    }
  }
</style>
