---
/**
 * HeroSection Component (2025 Edition)
 *
 * A modern, visually engaging hero section for the homepage that displays:
 * - Main heading with enhanced typography
 * - RSS feed link with improved styling
 * - Welcome message with visual emphasis
 * - Introduction text with improved readability
 * - Social links with enhanced visual presentation
 *
 * @component
 */

import { SOCIALS } from "@/config";
import Socials from "@/components/socials/Socials.astro";
import { Icon } from "astro-icon/components";

// Count active social links for conditional rendering
const socialCount = SOCIALS.filter(social => social.active).length;
---

<section id="hero" class="relative overflow-hidden">
  <!-- Decorative background elements -->
  <div
    class="absolute -right-16 -top-16 h-32 w-32 rounded-full bg-skin-accent/10 blur-2xl"
  >
  </div>
  <div
    class="absolute -left-24 top-1/2 h-48 w-48 rounded-full bg-skin-accent/5 blur-3xl"
  >
  </div>

  <div class="relative z-10 mx-auto max-w-content">
    <!-- Main heading with enhanced styling -->
    <div class="mb-8 flex items-center justify-between">
      <h1
        class="bg-gradient-to-r from-skin-accent to-skin-accent/80 bg-clip-text text-4xl font-black tracking-tight text-transparent sm:text-6xl"
        data-aos="fade-right"
        data-aos-duration="800"
      >
        Gesundes Leben
      </h1>

      <a
        target="_blank"
        href="/rss.xml"
        class="rss-link group flex items-center gap-2 rounded-full bg-skin-card p-2 shadow-sm transition-all duration-300 hover:bg-skin-accent/20 hover:shadow-md"
        aria-label="RSS Feed"
        title="RSS Feed"
        data-aos="fade-left"
      >
        <Icon
          name="tabler:rss"
          class="h-6 w-6 fill-skin-accent transition-transform duration-300 group-hover:scale-110"
        />
        <span class="sr-only">RSS Feed</span>
      </a>
    </div>

    <!-- Welcome message with enhanced styling -->
    <h2
      class="mb-6 text-4xl font-bold leading-tight tracking-tight sm:text-5xl"
      data-aos="fade-up"
      data-aos-delay="100"
    >
      Willkommen auf unserem Blog!
    </h2>

    <!-- Introduction text with improved typography and spacing -->
    <div
      class="prose-container max-w-7xl space-y-6"
      data-aos="fade-up"
      data-aos-delay="200"
    >
      <p class="text-lg leading-relaxed">
        Auf diesem Blog stellen dir Sandra & Kai Artikel über Gesundheitsthemen
        zur Verfügung und laden dich ein auf eine Reise zu mehr Gesundheit, die
        Spaß machen soll. <span
          class="hash inline-block transform transition-all duration-300 hover:scale-105"
          >sogehtgesund</span
        >
      </p>

      <p class="text-lg leading-relaxed">
        Wir betreiben den Blog als Privatpersonen, teilen aber unser
        Insider-Wissen, das wir uns in unserer langjährigen Arbeit als
        Therapeuten angeeignet haben. Das möchten wir auf einfache und
        verständliche Weise tun, denn wir finden, dass medizinische
        Zusammenhänge häufig zu kompliziert und für Laien schwer nachvollziehbar
        vermittelt werden. Deshalb verlinken wir medizinische Begriffe, wenn sie
        nicht vermeidbar sind, auf unser Glossar, wo du eine verständliche
        Erklärung findest.
      </p>

      <p class="text-lg leading-relaxed">
        Unser Ziel ist <strong class="font-bold text-skin-accent"
          >Empowerment für deine Gesundheit</strong
        >. Das bedeutet, wir möchten dich darin unterstützen, für deine
        Gesundheit aktiv zu werden und dir dafür unser Wissen und unsere
        Erfahrung kostenfrei zur Verfügung stellen. <span
          class="hash inline-block transform transition-all duration-300 hover:scale-105"
          >weildueswertbist</span
        >
      </p>
    </div>

    <!-- Social links with enhanced styling -->
    {
      socialCount > 0 && (
        <div class="social-wrapper" data-aos="fade-up" data-aos-delay="300">
          <div class="social-links">
            <Socials />
          </div>
        </div>
      )
    }
  </div>
</section>

<style>
  /* Hero section styles */
  #hero {
    @apply pb-12 pt-6;
  }

  /* Prose container for better typography */
  .prose-container p {
    @apply my-4 text-skin-base/90;
  }

  /* Social links wrapper */
  .social-wrapper {
    @apply mt-8 flex flex-col rounded-lg bg-skin-card/30 p-4 backdrop-blur-sm sm:flex-row sm:items-center;
  }

  /* Social links container */
  .social-links {
    @apply mb-1 mr-2 whitespace-nowrap sm:mb-0;
  }

  /* Hash tag styling */
  .hash {
    @apply cursor-pointer font-medium text-skin-accent;
    &::before {
      content: "#";
    }
  }

  /* Responsive adjustments */
  @media (max-width: 640px) {
    #hero {
      @apply pb-8 pt-4;
    }

    .prose-container p {
      @apply text-base;
    }
  }

  /* Reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .transform,
    [data-aos] {
      @apply transform-none transition-none;
    }
  }
</style>
