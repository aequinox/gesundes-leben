---
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import { Icon } from "astro-icon/components";
import type { CollectionEntry } from "astro:content";

/**
 * Component props interface with strict typing
 */
interface Props {
  /** The author entry from the content collection */
  author: CollectionEntry<"authors">;
  /** Size variant for the component */
  size?: "sm" | "lg";
  /** Additional CSS classes */
  classes?: string;
}

const { author, size = "sm", classes = "" }: Props = Astro.props;

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Precompute classes for better performance
const iconClasses = [
  "inline-block",
  "fill-skin-base",
  size === "sm" ? "h-5 w-5" : "scale-100",
];

const textClasses = ["italic", size === "sm" ? "text-sm" : "text-base"];
---

{
  author && (
    <div
      class:list={[
        "flex items-center gap-1 opacity-80 hover:opacity-100 transition-opacity",
        classes.trim(),
      ]}
    >
      <Icon
        name="tabler:user"
        class:list={iconClasses}
        aria-hidden="true"
        role="presentation"
      />
      <span class:list={textClasses}>
        <span class="sr-only">{t("author.from")}</span>
        <a
          href={`/author/${author.slug}`}
          class="hover:underline focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-skin-accent rounded-sm"
        >
          {author.data.name}
        </a>
      </span>
    </div>
  )
}
